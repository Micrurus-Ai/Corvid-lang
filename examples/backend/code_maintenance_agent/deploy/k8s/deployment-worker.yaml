apiVersion: apps/v1
kind: Deployment
metadata:
  name: code-worker
  namespace: code
  labels:
    app.kubernetes.io/name: code-maintenance-agent
    app.kubernetes.io/component: worker
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: code-maintenance-agent
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: code-maintenance-agent
        app.kubernetes.io/component: worker
    spec:
      containers:
        - name: code-worker
          image: ghcr.io/your-org/code-maintenance-agent:latest
          imagePullPolicy: IfNotPresent
          command:
            - /usr/local/bin/corvid
            - jobs
            - run
            - --source
            - examples/backend/code_maintenance_agent/src/main.cor
            - --state
            - /data/code.sqlite
            - --workers
            - "2"
            - --lease-ttl-ms
            - "60000"
            - --max-runtime-ms
            - "0"
          ports:
            - containerPort: 9090
              name: metrics
          envFrom:
            - configMapRef:
                name: code-config
            - secretRef:
                name: code-secrets
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          livenessProbe:
            tcpSocket:
              port: 9090
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 9090
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: code-data
