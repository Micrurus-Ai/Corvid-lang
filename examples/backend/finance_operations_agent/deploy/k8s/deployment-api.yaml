apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance-api
  namespace: finance
  labels:
    app.kubernetes.io/name: finance-operations-agent
    app.kubernetes.io/component: api
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: finance-operations-agent
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: finance-operations-agent
        app.kubernetes.io/component: api
    spec:
      containers:
        - name: finance-api
          image: ghcr.io/your-org/finance-operations-agent:latest
          imagePullPolicy: IfNotPresent
          command:
            - /usr/local/bin/corvid
            - run
            - --target=server
            - src/main.cor
            - --listen
            - 0.0.0.0:8080
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 9090
              name: metrics
          envFrom:
            - configMapRef:
                name: finance-config
            - secretRef:
                name: finance-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /schema
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /schema
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: finance-data
