# Adversarial: a `send_support_reply` call without a prior
# `approve SendSupportReply(...)` boundary must not compile.
# Slice 35V2-P42-D-LR-app-maturity-CustomerSupport-2.
# Expected: `corvid check` exits with E0101.

tool send_support_reply(req: String) -> String dangerous

agent bad() -> String:
    return send_support_reply("send support reply without approval")
