# Adversarial: a `close_ticket` call without a prior
# `approve CloseTicket(...)` boundary must not compile.
# Slice 35V2-P42-D-LR-app-maturity-CustomerSupport-2.
# Expected: `corvid check` exits with E0101.

tool close_ticket(req: String) -> String dangerous

agent bad() -> String:
    return close_ticket("close ticket without approval")
