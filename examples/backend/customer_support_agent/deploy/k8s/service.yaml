apiVersion: v1
kind: Service
metadata:
  name: support-api
  namespace: support
  labels:
    app.kubernetes.io/name: customer-support-agent
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: customer-support-agent
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: support-worker-metrics
  namespace: support
  labels:
    app.kubernetes.io/name: customer-support-agent
    app.kubernetes.io/component: worker
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: customer-support-agent
    app.kubernetes.io/component: worker
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: support-data
  namespace: support
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
