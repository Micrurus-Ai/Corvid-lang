[package]
name = "corvid-prompt-format"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "JSON Schema generation for Corvid prompt return types. Shared by the interpreter and native code generators so the language-LLM contract has one canonical implementation."

[dependencies]
corvid-ir = { workspace = true }
corvid-resolve = { workspace = true }
corvid-types = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
corvid-ast = { workspace = true }
