# Phase 40 phase-done audit — 2026-05-17

Third audit slice of the cross-phase verification round.

**Slice id:** `35V2-P40-A`.

## Headline

Phase 40 is the cleanest phase audited so far. 12 of 13 phase-done
items pass cleanly. The 1 open item (`review_queue.cost_of_being_wrong_ranking`
CLI ranking subcommand) is filed as launch-readiness.

## Phase 40 phase-done items and their state

| # | Item | State | Action |
|---|------|-------|--------|
| 1-7 | Scope bullets (OTel export, `corvid observe` command, evals from production traces, drift+regression reports, human-review queues, AI-native integration, lineage IDs) | Slice checklist `40A-40J` + sub-slices all `[x]`. Reference implementations under `crates/corvid-runtime/src/{lineage,lineage_redact,lineage_incidents,otel_sdk_export,review_queue}.rs` + `crates/corvid-cli/src/{observe_helpers_cmd,eval_cmd}/`. | Tick scope bullets in 35V2-P40-C. |
| 8 | OTel conformance test against docker-compose Jaeger collector; spans carry `corvid.guarantee_id`, `corvid.cost_usd`, `corvid.approval_id`, `corvid.replay_key` attributes | **Present.** In-process OTLP receiver test (`sdk_exporter_reaches_in_process_otlp_receiver`) exercises the wire path. Three attribute tests pin the named attribute set (`span_attributes_include_corvid_named_keys`, `span_name_uses_corvid_prefix_with_kind`, `span_kind_maps_lineage_to_otel`). The full docker-compose Jaeger harness is documented at `docs/operations/observability-conformance.md` (out-of-process integration documented for operators; the property-correctness tests cover the wire shape). | None — registered + tested. |
| 9 | Seven `observability.*` / `eval.*` / `review_queue.*` registry rows, each Static or RuntimeChecked with positive + adversarial test refs | **6/7 satisfy the bar.** `observability.otel_conformance`, `observability.lineage_completeness`, `observability.redaction_determinism`, `observability.contract_aware_grouping`, `eval.drift_attribution`, `eval.promotion_signed_lineage` all RuntimeChecked with positive + adversarial test refs (each test ref verified by `every_test_ref_resolves_to_a_real_test_function`). 1 OutOfScope: `review_queue.cost_of_being_wrong_ranking` — review-queue envelopes ship at `corvid_runtime::review_queue` but the `corvid review-queue list --rank=cost-of-being-wrong` CLI subcommand is not yet wired. | **35V2-P40-B:** add Phase-40 presence sentinel (parallel to Phase-38 + Phase-39 sentinels); tighten the OutOfScope reason to name the specific filing slice. **35V2-P40-C-LR-review-queue-ranking-cli:** file the CLI subcommand as launch-readiness. |
| 10 | Redaction adversarial test: promote a trace containing fake SSNs → assert zero regex matches against an SSN pattern in the resulting fixture file | **Present.** `redaction_removes_obvious_secrets_from_serialized_lineage` in `crates/corvid-runtime/src/lineage_redact.rs:264` seeds a span with `"Bearer sk-live-123 for 123-45-6789"` and asserts `!json.contains("123-45-6789")` after redaction. `contains_ssn_like_id` heuristic exists in the same file. | None — landed at slice 40 time, verified by audit. |
| 11 | Drift attribution test: synthetically swap (a) model fingerprint, (b) prompt, (c) retrieval index → assert the explainer reports each contribution | **Present.** Two tests in `crates/corvid-cli/src/observe_helpers_cmd/eval_drift.rs`: `drift_explain_attributes_model_swap` (positive) + `drift_explain_surfaces_residual_when_status_flips_alone` (adversarial, residual-attribution path). | None. |
| 12 | AI helpers: `corvid observe explain` (RAG-grounded) + `corvid eval promote` (agentic) | **Both present.** `corvid observe explain` shipped at `crates/corvid-cli/src/observe_helpers_cmd/observe_explain.rs` (247 lines + 2 round-trip tests). `corvid eval promote` shipped at `crates/corvid-cli/src/eval_cmd/promote.rs` — invoked via `corvid eval promote <trace.lineage.jsonl> --promote-out <DIR>`. | None — both AI helpers shipped (contrast with Phase 38 and Phase 39, where the equivalent helpers had to be filed as launch-readiness). |
| 13 | `benches/comparisons/observability.md` side-by-side benchmark file | **Present.** | None. |

## Systemic finding (continuing from P38 + P39)

The "OutOfScope reasons reference promotion slices that shipped without promoting" pattern is **less severe in P40** than it was in P38 or P39:

- P38: 38K promised to promote 3 rows; shipped without promoting → audit caught + tightened.
- P39: 39L promised to promote 6 rows; shipped without promoting → audit caught + tightened.
- P40: only 1 OutOfScope row, and its reason names "a follow-up slice" rather than a specific shipped-but-didn't-promote slice → audit just needs to name the actual filing slice (35V2-P40-C-LR-review-queue-ranking-cli).

Worth recording: phases that shipped *fewer* OutOfScope rows in the first place (P40 has 1; P39 had 6; P38 had 3) avoid the recurring drift. The cleaner each phase ships, the less the verification round has to do. Pattern to pin in learnings.md when verification round closes.

## Correction-slice summary

| Slice | Action | Estimate | Class |
|---|---|---|---|
| 35V2-P40-B | Add `phase_40_required_registry_ids_all_present` sentinel (parallel to Phase-38 + Phase-39 sentinels); tighten `review_queue.cost_of_being_wrong_ranking` OutOfScope reason to name the specific filing | ~30 min | Sentinel + Registry |
| 35V2-P40-C | Tick Phase 40 ROADMAP scope bullets + items 8-13 (all clean) | ~15 min | ROADMAP |
| 35V2-P40-C-LR-review-queue-ranking-cli | File `corvid review-queue list --rank=cost-of-being-wrong` CLI subcommand as launch-readiness — promotes `review_queue.cost_of_being_wrong_ranking` to RuntimeChecked when shipped | Filed | Filed |

**Total in-flight correction work:** ~45 min — by far the smallest Phase 38-42 audit-correction tail so far.
