# Phase 38 phase-done audit — 2026-05-17

First slice of the cross-phase verification round (Phase 35V Track 2
pattern applied to Phases 38-42). Audit-only; corrective slices land
separately.

**Slice id:** `35V2-P38-A`.

**Rules used** (per the verification-round pre-phase chat, 2026-05-17):

- Strict ROADMAP order: Phase 38 first, then 39, 40, 41, 42.
- Drift threshold: registered-guarantee drift → correction slice now;
  claim-coverage gaps → correction slice now; benchmark files → land
  now; AI helpers > 1 day → file as launch-readiness; user-facing
  documentation drift → correction slice now (added during audit).
- Sentinel only when drift was real AND drift mode is non-obvious.

## Phase 38 phase-done items and their state

| # | Item | State | Action |
|---|------|-------|--------|
| 1 | `validate_signed_claim_coverage` recognises `@retry`, `@idempotency`, `@replayable`, `job`, `schedule`, `await_approval` as declared contracts | **Mixed.** `schedule` wires `jobs.cron_schedule_durable`; `@replayable` wires `replay.deterministic_pure_path`. `@retry` / `@idempotency` / `job` / `await_approval` do not exist as Corvid surface (no parser keyword, no AST node, not in `corvid-syntax` token list). | **35V2-P38-F:** reword the requirement honestly. The shipped surface is `agent` + `schedule` + `@replayable`; that surface already wires its claim ids correctly. Filing the aspirational keyword set (`job` / `@retry` / `@idempotency` / `await_approval`) as **35V2-P38-H** (post-v1.0 follow-up). |
| 2 | Eight `jobs.*` registry rows, each Static or RuntimeChecked with positive + adversarial test refs | **Mixed.** 4 satisfy the bar (`jobs.idempotency_key_uniqueness`, `jobs.lease_exclusivity`, `jobs.durable_resume`, `jobs.cron_dst_correct`). 3 are OutOfScope with honest reasons (`jobs.retry_budget_bound`, `jobs.approval_wait_resume`, `jobs.loop_bounds_enforced`). 1 is missing (`jobs.replayable_side_effects`). The registry has one bonus row (`jobs.cron_schedule_durable`) outside the ROADMAP list — extra coverage, not drift. | **35V2-P38-B:** add `jobs.replayable_side_effects` row (OutOfScope with honest reason about side-effect-replay being a host-side runtime property; Phase 43 promotes when the launch-readiness sweep wires it end-to-end). **35V2-P38-D:** investigate the 3 existing OutOfScope rows — for each, either promote to Static/RuntimeChecked if slice 38K shipped enough surface, or tighten the OutOfScope reason to reference the specific v1.0 launch-readiness work that promotes it. |
| 3 | SIGKILL crash-recovery integration test | **Present.** `crates/corvid-runtime/tests/durability_corpus.rs::t38l_d3_checkpoints_survive_unclean_shutdown` referenced by `jobs.durable_resume`. Property-equivalent stand-in for SIGKILL (worker drops mid-lease before completion, fresh runtime resumes); the literal subprocess-SIGKILL harness is a post-v1.0 hardening item per the existing slice 38L commentary. Acceptance honest. | None. |
| 4 | 4-concurrent-worker / 100-jobs-same-key idempotency adversarial test | **Present.** `crates/corvid-runtime/tests/durability_corpus.rs::t38l_d1_four_workers_collapse_to_one_row` referenced by `jobs.idempotency_key_uniqueness`. | None. |
| 5 | DST cron test (2:30am spring-forward + 1:30am fall-back) | **Present.** `t38l_d2_dst_spring_forward_is_deterministic` + `t38l_d2_dst_fall_back_is_monotonic` referenced by `jobs.cron_dst_correct`. | None. |
| 6 | Replay-quarantine test (replay old job trace, assert no real provider call) | **Missing.** No test grep hits in `corvid-runtime`. The shipped queue runtime + Phase 21 replay infrastructure both exist; an integration test that wires them together is the gap. | **35V2-P38-C:** add the test. Estimate: a few hours. Uses the existing mock LLM call counter the SIGKILL test already exercises. |
| 7 | `corvid jobs explain` AI helper (RAG-grounded over typed trace) | **Missing.** No `JobsExplain` command in CLI. | **35V2-P38-G:** file as launch-readiness (AI helper > 1 day per the verification-round rules). Lands during Phase 43 launch-readiness window. |
| 8 | `benches/comparisons/jobs_durability.md` side-by-side benchmark file | **Present.** Compares Corvid against Celery + BullMQ + Temporal SDK with governance-line delta. | None. |

## Surfaced finding (not in the original audit scope)

`docs/guides/jobs.md` is a user-facing guide and uses the aspirational
`job` keyword in two code examples (lines 23, 66). A v1.0 user
copy-pasting from the guide gets a parse error. This is launch-
blocking drift, not cosmetic. The reference apps under
`examples/backend/` already use the shipped `agent` + `schedule`
surface — so the docs lag the apps, not the other way around.

**35V2-P38-E:** rewrite `docs/guides/jobs.md` to use the shipped
surface. Same rewrite applies to the Phase 38 ROADMAP
developer-flow snippet (which is internal, less urgent, but worth
fixing in the same commit since the drift mode is identical).

## Correction-slice summary

| Slice | Action | Estimate | Class |
|---|---|---|---|
| 35V2-P38-B | Add `jobs.replayable_side_effects` registry row (OutOfScope + honest reason) | 30 min | Registry |
| 35V2-P38-C-deferred | Replay-quarantine integration test (FILED as launch-readiness) — recon under this slice found the queue runtime and Phase 21 replay are separate layers with no `replay_job` wiring. The work is the integration design + implementation, not a 2-hour test write-up. Filed to land in Phase 43 launch-readiness window. The registry row `jobs.replayable_side_effects` OutOfScope reason was tightened in 35V2-P38-B to name this scope honestly. | ~2-4 days when it lands | Filed |
| 35V2-P38-D | Investigate + correct 3 existing OutOfScope `jobs.*` rows (promote or tighten reason) | ~3 hr | Registry |
| 35V2-P38-E | Rewrite `docs/guides/jobs.md` + Phase 38 ROADMAP developer-flow snippet to match shipped `agent` + `schedule` + `@replayable` surface | ~2 hr | Docs |
| 35V2-P38-F | Rewrite Phase 38 phase-done claim-coverage item to reference shipped constructs | 30 min | ROADMAP wording |
| 35V2-P38-G | File `corvid jobs explain` as launch-readiness slice | 5 min | Filed |
| 35V2-P38-H | File `job` keyword + `@retry`/`@idempotency`/`await_approval` syntax as post-v1.0 follow-up | 5 min | Filed |

**Total in-flight correction work:** ~6 hours (was ~8 before 35V2-P38-C was filed; the recon-revealed wiring gap pushed C to launch-readiness).

## Sentinel discipline

Two sentinels worth adding under the audit-round rules:

- **Docs-as-code drift gate.** A CI step that compiles every Corvid
  code block in `docs/guides/*.md` through `corvid check`. Catches
  the exact drift mode this audit surfaced (user-facing docs that
  reference language surface that doesn't ship). Lands in 35V2-P38-E
  alongside the rewrite.
- **Registry-row presence sentinel for required Phase 38 ids.** A
  guarantees-crate test that asserts the 8 ids in the Phase 38
  phase-done list are all registered. Catches the `jobs.replayable_side_effects`
  drift mode going forward. Lands in 35V2-P38-B alongside the new row.

## Tick after corrections land

When 35V2-P38-B through F land:
- Tick item 1, 2, 6 in the ROADMAP Phase 38 phase-done checklist.
- Items 3, 4, 5, 8 tick now (clean as audited).
- Items 7 (AI helper) and the deferred language surface stay as the
  remaining open items in the phase-done list; both reference their
  filed follow-up slices.
