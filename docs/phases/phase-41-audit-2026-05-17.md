# Phase 41 phase-done audit — 2026-05-17

Fourth audit slice of the cross-phase verification round.

**Slice id:** `35V2-P41-A`.

## Headline

Phase 41 is strong on the adversarial corpus (7/7 named threats
covered) and on most registry rows (4/6 RuntimeChecked). Two
OutOfScope rows + the AI-helper trio + the connector-return
provenance question are the open items. Filing tail similar to
P38/P39, smaller than that.

## Phase 41 phase-done items and their state

| # | Item | State | Action |
|---|------|-------|--------|
| 1-7 | Scope bullets (Gmail, M365, Calendar, Slack, Linear+GitHub, Local files, connector manifest, mock suite) | Slice checklist `41A-41J` + sub-slices all `[x]`. Reference implementations under `crates/corvid-connector-runtime/src/{gmail,gmail_real,ms365,calendar,slack,github_real,linear,files,manifest,oauth2_refresh,webhook_verify,runtime}.rs`. | Tick scope bullets in 35V2-P41-C. |
| 8 | `validate_signed_claim_coverage` recognises `connector` / `scopes` / `rate_limit` / `redact` / `webhook_signed_by` as declared contracts | **Mixed.** Same pattern as P38 + P39: these aren't AST surface. Connectors are configured by Rust data (`shipped_manifests` slice in `corvid-connector-runtime/src/manifest.rs`), not by source-level Corvid declarations. The runtime + CLI surfaces (`corvid connectors list/check/run/oauth/verify-webhook`) ship; the source-level form is post-v1.0 ergonomic sugar. | **35V2-P41-F:** reword the requirement to match shipped surface. **35V2-P41-I:** file the aspirational source-level `connector` / `scopes` / `rate_limit` / `redact` / `webhook_signed_by` keywords as post-v1.0 syntax sugar (same shape as P38-H / P39-I). |
| 9 | Six `connector.*` registry rows, each Static or RuntimeChecked with positive + adversarial test refs | **4/6 satisfy the bar.** `connector.scope_minimum_enforced` (✅ RuntimeChecked, 3 adversarial threat-corpus refs), `connector.rate_limit_respects_provider` (✅ RuntimeChecked), `connector.webhook_signature_verified` (✅ RuntimeChecked, 3 adversarial refs across github/slack/linear), `connector.replay_quarantine` (✅ RuntimeChecked). 2 OutOfScope: `connector.write_requires_approval` (manifest declares write effects but the source-level `connector ... uses dangerous` surface that would let typecheck refuse a call without lexical-scope approve is not yet wired), `connector.contract_drift_detected` (slice 41L wired the `corvid connectors check` validation half; the `--live` drift-narration path is gated behind `CORVID_PROVIDER_LIVE=1` and explicitly returns an `Err` directing to slice 41M-C). | **35V2-P41-B:** add Phase-41 presence sentinel; tighten the 2 OutOfScope reasons to name specific filings. |
| 10 | Mock ≡ replay ≡ real: each connector ships all three modes; CI runs the same integration test in mock by default and real behind `CORVID_PROVIDER_LIVE=1` | **Partial.** Each connector exposes all three modes via the runtime; the `connector_fixtures.rs` integration tests run in mock by default. The CI matrix that runs the same test under `CORVID_PROVIDER_LIVE=1` against real providers is **not visible in `.github/workflows/`** — the gate exists at runtime but the "CI runs both" part is unwired. | **35V2-P41-E-LR-live-provider-ci-matrix:** file as launch-readiness — the CI matrix needs provider credentials in GitHub secrets, which is an operational gate, not a code gate. |
| 11 | Adversarial corpus enumerates per-connector named threats: token-scope escalation, cross-tenant message access, refresh-token replay after revocation, malformed JSON body, 429/5xx retries with `Retry-After`, expired OAuth state, webhook signature forgery | **All 7 covered.** 14 tests across `tests/threat_corpus.rs`: t1 × 3 (scope rejection), t2 × 3 (tenant rejection), t3 (oauth refresh after revocation), t4 (malformed/unknown write), t5 (rate-limited + retry-after), t6 (oauth expired refresh), t7 × 3 (webhook signature/replay across github/slack/linear). | None. |
| 12 | Provenance test: every connector return is `Grounded<T>` whose provenance is the provider's record id; downstream code that strips provenance fails typecheck under `grounded.propagation_across_calls` | **Not shipped.** Connector returns are plain JSON values (`serde_json::Value`), not `Grounded<T>`. The agent calling the connector is responsible for any wrapping. The ROADMAP item assumes a connector-side `Grounded<T>` return shape that requires Corvid source-level `connector` blocks (which is the post-v1.0 syntax sugar from item 8). | **35V2-P41-G-LR-connector-grounded-returns:** file as launch-readiness — the connector-return-wrapping behaviour follows directly from the source-level `connector ... grounded` declaration, which itself is filed in 35V2-P41-I. This row depends on 35V2-P41-I shipping first; chain documented in the filing. |
| 13 | AI helpers: `corvid connectors mock-fixture-gen` (generative) + `corvid connectors check --live` drift narrator (RAG-grounded) + `corvid connectors fail-sim` (adversarial) | **0/3 shipped.** `check --live` returns an explicit `Err` directing to slice 41M-C; `mock-fixture-gen` and `fail-sim` are unrecognized subcommands. | **35V2-P41-H-LR-connectors-ai-helpers:** file all three as launch-readiness (each is its own slice but they share a filing umbrella; the umbrella tracks the 3 sub-slices). |
| 14 | `benches/comparisons/connectors.md` | **Present.** | None. |

## Systemic finding (continuing from P38/P39/P40)

The "OutOfScope reasons need tightening" pattern returns in P41 (2 rows). Same shape as P38/P39: the OutOfScope row names a future slice for promotion (41L, 41M), the slice shipped its stated surface, but the row's promotion wasn't ticked. Audit catches.

The "AI helpers absent → filed as launch-readiness" pattern is the strongest predictor across all four phases:
- P38: 1 AI helper missing (filed)
- P39: 2 AI helpers missing (filed)
- P40: 0 AI helpers missing (both shipped)
- P41: 3 AI helpers missing (filed under an umbrella)

P40's all-AI-helpers-shipped pattern shows it's achievable when the phase prioritised it. Phases that filed instead deferred; the launch-readiness tail grows accordingly.

## Correction-slice summary

| Slice | Action | Estimate | Class |
|---|---|---|---|
| 35V2-P41-B | Add `phase_41_required_registry_ids_all_present` sentinel; tighten 2 OutOfScope reasons (`connector.write_requires_approval` → references 35V2-P41-I post-v1.0; `connector.contract_drift_detected` → references 35V2-P41-D-LR-connector-drift-narration). | ~30 min | Sentinel + Registry |
| 35V2-P41-C | Tick Phase 41 ROADMAP scope bullets + items 8-14 (with filing references) | ~20 min | ROADMAP |
| 35V2-P41-D-LR-connector-drift-narration | File `--live` drift-narration path (and the AI-helper narrator on top of it) as launch-readiness | Filed | Filed |
| 35V2-P41-E-LR-live-provider-ci-matrix | File CI matrix that runs integration tests with `CORVID_PROVIDER_LIVE=1` against real providers as launch-readiness (needs operational provider credentials) | Filed | Filed |
| 35V2-P41-F | Reword Phase 41 ROADMAP claim-coverage item 8 to match shipped surface | included in C | (folded) |
| 35V2-P41-G-LR-connector-grounded-returns | File connector-return-wrapping-in-`Grounded<T>` behaviour as launch-readiness (depends on 35V2-P41-I) | Filed | Filed |
| 35V2-P41-H-LR-connectors-ai-helpers | File all 3 AI helpers (`mock-fixture-gen`, `check --live` narrator, `fail-sim`) under one filing umbrella | Filed | Filed |
| 35V2-P41-I-post-v1.0-connector-syntax-sugar | File `connector`/`scopes`/`rate_limit`/`redact`/`webhook_signed_by` source-level keywords as post-v1.0 (same shape as P38-H / P39-I) | Filed | Filed |

**Total in-flight correction work:** ~50 min.
